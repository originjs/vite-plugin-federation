import React from 'react'
import dom from 'react-dom'

console.log(dom)
const style = {
  background: '#a3e077',
  color: '#FFFFFF',
  padding: 12
}

const Button = () => <button style={style}> Rollup Remote React Button</button>

export default Button
